Требования 
requests>=2.28.0
beautifulsoup4>=4.11.0
pandas>=1.5.0
openpyxl>=3.0.10  # Для работы с Excel
Пояснение:
requests — основная библиотека для выполнения HTTP-запросов.
beautifulsoup4 — для парсинга HTML-страниц.
pandas — для работы с данными (например, сохранение в CSV или обработка таблиц).
openpyxl — для сохранения данных в формат Excel.